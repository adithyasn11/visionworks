fastapi>=0.110.0
uvicorn[standard]>=0.28.0
ultralytics>=8.1.0
opencv-python-headless>=4.9.0
opencv-contrib-python>=4.9.0
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
shapely>=2.0.0
scipy>=1.12.0
pydantic>=2.6.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
websockets>=12.0
jinja2>=3.1.0
reportlab>=4.1.0
pandas>=2.2.0
onnxruntime>=1.17.0
supabase>=2.3.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── Per-employee identity (IDENTITY_TRACKING_PLAN.md Phase B) ────────────────
# OSNet person re-identification. `gdown` and `tensorboard` are transitive:
# torchreid's package __init__ imports its whole training stack, so both are
# needed even though only the inference model is used.
torchreid>=0.2.5
gdown>=4.7.0
tensorboard>=2.14.0
